import json
import numpy as np


#written by Dylienne Vrije Universiteit Amsterdam
#PROV file from PROV store with JSON conversion
file= read.json(...)

# create file with all prov:entities from W3 websites

#read document
def readDocument():
    open "path" as JSONPYTHON.readlines()

#identify all entities regarding prov:x in JSON
def extractentity():
    number = []
    for x in file:
        if line[0:10]== "prov:X"
            number.append(line)
            return number

#count entities types in repositories
def countentities()
    counter = 0
    for number in extractentity():
        if number == number:
            print()


def calculateInstances():
    amountofinstances= [list]

def histogram():
    x= countentities().counter
    y= len(calculateInstances())
        np.histogram2d(x,y)

#percentage and piecharts in entity distributions

#program

readDocument()
extractentity()

np.histogram2d()
